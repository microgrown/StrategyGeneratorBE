{
    "name": "BarRangeAboveStd",
    "type": "Entry",
    "inputVariables": {
        "numDevs": "2",
        "lookback": "15"
    },
    "localVariables": {
        "rrange": {
            "type": "double",
            "init": "0.0"
        },
        "rangeMean": {
            "type": "double",
            "init": "0.0"
        },
        "rangeStd": {
            "type": "double",
            "init": "0.0"
        }
    },
    "classMembersHook": "",
    "startOfFileHook": "",
    "preConditionHook": "rrange = high[0] - low[0];\nrangeMean = 0.0;\nfor (int i = 0; i < (int)lookback; ++i) {\n    rangeMean += high[i] - low[i];\n}\nrangeMean /= (int)lookback;\nrangeStd = 0.0;\nfor (int i = 0; i < (int)lookback; ++i) {\n    double d = (high[i] - low[i]) - rangeMean;\n    rangeStd += d * d;\n}\nrangeStd = std::sqrt(rangeStd / (int)lookback);",
    "longCondition": "rrange > numDevs * rangeStd + rangeMean",
    "shortCondition": "rrange > numDevs * rangeStd + rangeMean",
    "postConditionHook": "",
    "endOfFileHook": ""
}
