{
    "name": "HighestHighBreakout",
    "type": "Entry",
    "inputVariables": {
        "lookback": "15"
    },
    "localVariables": {
        "highestHigh": {
            "type": "double",
            "init": "0.0"
        },
        "lowestLow": {
            "type": "double",
            "init": "0.0"
        }
    },
    "classMembersHook": "",
    "startOfFileHook": "",
    "preConditionHook": "highestHigh = high[0];\nlowestLow = low[0];\n// Mirrors EasyLanguage's Extremes(), which Highest()/Lowest() call: seeded\n// at bar 0, scanning bars 1..Length-1, strict comparison (so a tie keeps the\n// earliest bar). Extremes is EL SOURCE, so its comparison carries the EL\n// tolerance -- el_gt/el_lt, as in Breakout (the reference).\nfor (int i = 1; i < (int)lookback; ++i) {\n    if (el_gt(high[i], highestHigh)) highestHigh = high[i];\n    if (el_lt(low[i], lowestLow)) lowestLow = low[i];\n}",
    "longCondition": "el_eq(high[0], highestHigh)",
    "shortCondition": "el_eq(low[0], lowestLow)",
    "postConditionHook": "",
    "endOfFileHook": ""
}
