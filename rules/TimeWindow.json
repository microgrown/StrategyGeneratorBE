{
    "name": "TimeWindow",
    "type": "Entry",
    "inputVariables": {
        "startTime": "1600",
        "endTime": "2300"
    },
    "localVariables": {
        "barSeconds": {
            "type": "int",
            "init": "0"
        },
        "barHHMM": {
            "type": "int",
            "init": "0"
        }
    },
    "classMembersHook": "",
    "startOfFileHook": "",
    "preConditionHook": "// EL Time is the bar's closing time as HHMM (EL_FEATURES.md `time`, VERIFIED:\n// exchange clock, wall-clock close, no timezone conversion; a bar closing at\n// midnight reads 0). ctx.Time(0) is that same close timestamp, so HHMM is its\n// seconds-of-day re-packed -- the TimeOfDayExit derivation.\nbarSeconds = seconds_of_day(ctx.Time(0));\nbarHHMM = (barSeconds / 3600) * 100 + (barSeconds % 3600) / 60;",
    "longCondition": "el_ge((double)barHHMM, startTime) && el_le((double)barHHMM, endTime)",
    "shortCondition": "el_ge((double)barHHMM, startTime) && el_le((double)barHHMM, endTime)",
    "postConditionHook": "",
    "endOfFileHook": ""
}
