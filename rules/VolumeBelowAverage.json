{
    "name": "VolumeBelowAverage",
    "type": "Entry",
    "inputVariables": {
        "lookback": "10"
    },
    "localVariables": {
        "avgVolume": {
            "type": "double",
            "init": "0.0"
        }
    },
    "classMembersHook": "",
    "startOfFileHook": "",
    "preConditionHook": "avgVolume = 0.0;\nfor (int i = 0; i < (int)lookback; ++i) {\n    avgVolume += (double)volume[i];\n}\navgVolume /= (int)lookback;",
    "longCondition": "(double)volume[0] < avgVolume",
    "shortCondition": "(double)volume[0] < avgVolume",
    "postConditionHook": "",
    "endOfFileHook": ""
}
