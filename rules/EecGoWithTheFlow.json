{
    "name": "EecGoWithTheFlow",
    "type": "Entry",
    "inputVariables": {
        "openloss": "1000"
    },
    "localVariables": {},
    "classMembersHook": "",
    "startOfFileHook": "",
    "preConditionHook": "// Entry & Exit Confessions #1 (Go With The Flow), verbatim: 1-bar momentum,\n// allowed to reverse only when flat or when the open position has lost more\n// than openloss. OpenPositionProfit is net of entry-side costs (verified).",
    "longCondition": "el_gt(close[0], close[1]) && (el_lt(ctx.OpenPositionProfit(), -openloss) || el_eq(ctx.MarketPosition(), 0))",
    "shortCondition": "el_lt(close[0], close[1]) && (el_lt(ctx.OpenPositionProfit(), -openloss) || el_eq(ctx.MarketPosition(), 0))",
    "postConditionHook": "",
    "endOfFileHook": ""
}
