{
    "name": "AtrProfitTarget",
    "type": "Exit",
    "inputVariables": {
        "lookback": "15",
        "multiple": "3"
    },
    "localVariables": {
        "atr": {
            "type": "double",
            "init": "0.0"
        }
    },
    "classMembersHook": "",
    "startOfFileHook": "",
    "preConditionHook": "atr = 0.0;\nfor (int i = 0; i < (int)lookback; ++i) {\n    double hi = high[i] > close[i + 1] ? high[i] : close[i + 1];\n    double lo = low[i] < close[i + 1] ? low[i] : close[i + 1];\n    atr += hi - lo;\n}\natr /= (int)lookback;",
    "longCondition": "ctx.OpenPositionProfit() > multiple * atr * ctx.BigPointValue()",
    "shortCondition": "ctx.OpenPositionProfit() > multiple * atr * ctx.BigPointValue()",
    "postConditionHook": "",
    "endOfFileHook": ""
}
