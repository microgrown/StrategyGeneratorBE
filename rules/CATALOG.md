# Rule catalog

One line per rule in `rules/`. Read this before writing a new rule — it is the
cheap first pass of the duplicate check. Open the full JSON only for a rule that
looks like a plausible match.

**Sides** — `mirrored` means the short condition is the long condition reflected
(`>` becomes `<`, highest becomes lowest); `same` means the rule is direction-
agnostic and both sides are identical text, which is correct for anything phrased
in profit, volume, elapsed bars, or absolute range.

**Reaches back** — the deepest bar index the rule can touch, as an expression over
its inputs. Max Bars Back for a strategy must cover the largest of these across
every placement, evaluated at each input's *stop* value.

| Rule | Type | Sides | Reaches back | Inputs | Fires when |
|---|---|---|---|---|---|
| `Momentum` | Entry | mirrored | `lookback` | `lookback(200)` | Close is above the close N bars ago |
| `DualMomentum` | Entry | mirrored, one input per side | `max(longLookback, shortLookback)` | `longLookback(20)`, `shortLookback(20)` | Long side: close is above the close N bars ago. Short side: close is below the close M bars ago. `Momentum` with the two sides split onto separate inputs |
| `MomentumChange` | Entry | mirrored | `lookback + 1` | `lookback(50)` | Momentum turns positive *this* bar — above N bars ago now, but it was not on the previous bar |
| `MomentumConsecutiveBars` | Entry | mirrored | `lookback + consecutiveBars - 1` | `lookback(200)`, `consecutiveBars(10)` | Momentum has held positive for N consecutive bars |
| `Breakout` | Entry | mirrored | `lookback - 1` | `lookback(50)` | Close is at or above the highest close of the last N bars |
| `BarRangeAboveStd` | Entry | same | `lookback - 1` | `numDevs(2)`, `lookback(15)` | This bar's high−low range exceeds the N-bar mean range plus K standard deviations |
| `VolumeBelowAverage` | Entry | same | `lookback - 1` | `lookback(10)` | This bar's volume is below its own N-bar average |
| `AdxBelowThreshold` | Entry | same | `lookback` | `lookback(14)`, `threshold(25)` | The 14-bar ADX (MW's `WFSafe_ADX`, bit-exact port) is below the threshold — a low-trend regime filter, identical both sides |
| `AtrBandBreakout` | Entry | mirrored | `max(lookback - 1, atrLookback + 1)` | `lookback(20)`, `atrLookback(15)`, `atrMultiple(1)` | Close is at or beyond the ATR band around the Donchian midline: base = (highest high + lowest low of N bars)/2, long at close ≥ base + K×ATR, short at close ≤ base − K×ATR |
| `CloseAboveAverage` | Entry | mirrored | `lookback - 1` | `lookback(50)` | Close is above (long) / below (short) its own N-bar average |
| `ClosePercentileRank` | Entry | mirrored | `lookback - 1` | `rankPercentile(10)`, `lookback(100)` | Close ranks in the top (long) / bottom (short) X% of the last N closes — ties share the better rank; at X ≈ 100/N the long side degenerates to `Breakout` |
| `ConsecutiveUpBars` | Entry | mirrored | `consecutiveBars - 1` | `consecutiveBars(3)` | Each of the last N bars closed above (long) / below (short) its own open — a tie bar fails both sides |
| `LaggedHigherHigh` | Entry | mirrored | `lookback + 1` | `lookback(1)` | The bar N bars ago made a higher high than the bar before it (long) / a lower low (short) |
| `CloseAbovePriorHigh` | Entry | mirrored | `lookback` | `lookback(1)` | Close is above the high (long) / below the low (short) of the bar N bars ago — at N=1, a close outside the prior bar's range |
| `AtrAboveAverage` | Entry | same | `atrLookback + 1` | `atrLookback(15)`, `lookback(20)` | The ATR exceeds its own N-bar average — a volatility-expansion filter; the average runs over the ATR variable's zero-padded warm-up history, so it passes easily early on |
| `AtrRising` | Entry | same | `max(atrLookback + 1, max(nearLag, farLag))` | `atrLookback(15)`, `nearLag(1)`, `farLag(10)` | The ATR as of X bars ago is above the ATR as of Y bars ago — a volatility-trend filter; both readings come from the ATR variable's zero-padded warm-up history, so for the first max(X, Y) evaluated bars the older reading is 0 and the rule passes trivially |
| `AtrFastAboveSlow` | Entry | same | `max(fastLookback, slowLookback) + 1` | `fastLookback(5)`, `slowLookback(20)` | The X-bar ATR exceeds the Y-bar ATR — a volatility-expansion filter from two independent `WFSafe_AvgTrueRange` call sites; identical both sides |
| `StopLoss` | Exit | same | 0 | `stopLoss(1000)` | Open loss exceeds X dollars |
| `TakeProfit` | Exit | same | 0 | `takeProfit(1000)` | Open profit exceeds X dollars |
| `AtrProfitTarget` | Exit | same | `lookback + 1` | `lookback(15)`, `multiple(3)` | Open profit exceeds K × the N-bar average true range, converted to dollars |
| `ProfitProtector` | Exit | same | 0 | `maxProfit(1500)`, `retracement(0.5)` | Open profit peaked above X, then gave back to a fraction of that peak |
| `TrailingStop` | Exit | same | 0 | `trailStop(1000)` | Open profit has fallen more than X dollars below the position's peak profit |
| `AtrTrailingStop` | Exit | same | `lookback + 1` | `lookback(15)`, `multiple(3)` | Open profit has fallen more than K × the N-bar average true range (in dollars) below the position's peak profit |
| `NumBars` | Exit | same | 0 | `numBarsExit(50)` | The position has been open N bars or more |
| `MonthlyProfitTarget` | Switch | same | 0 | `ProfitTarget(5000)` | Equity gained since the start of the calendar month exceeds the target — blocks entries and forces flat |
| `DayOfWeekEntry` | Entry | one input per side | 0 | `dayLong(4)`, `dayShort(3)` | Long when the bar's close date falls on weekday X, short on weekday Y — EL numbering, 0 = Sunday .. 6 = Saturday |
| `TrueRangeAvgRatio` | Entry | same | `lookback * slowFactor` | `lookback(30)`, `slowFactor(2)`, `multiple(1)` | The plain N-bar mean of True Range exceeds K × the plain (N × F)-bar mean — EL's fresh `Average(TrueRange, n)`, not the `WFSafe_` rolling sum, so not `AtrFastAboveSlow`; negate for the book's "volatility contraction" form |
| `VolumeAboveAverageMultiple` | Entry | same | `lookback - 1` | `lookback(35)`, `multiple(2)` | This bar's total ticks (up + down, `ctx.Ticks`, not the up-tick `volume` alias) exceed K × their own N-bar average; the average runs over an EL variable's zero-padded warm-up history |
| `BodyBelowAtrFraction` | Entry | same | `atrLookback + 1` | `atrLookback(14)`, `fraction(0.2)` | The bar's body, \|close − open\|, is smaller than F × the N-bar ATR (`WFSafe_AvgTrueRange` rolling form) — a narrow-body filter, identical both sides |
| `TakeProfitWithRatioStop` | Exit | same | 0 | `target(3000)`, `stopRatio(0.5)` | Open profit exceeds X dollars, or open loss exceeds R × X — one parameter drives both legs, which is why it is not `TakeProfit` + `StopLoss` |
| `RsiBelowThreshold` | Entry | mirrored | `lookback` | `lookback(14)`, `threshold(30)` | Long when the N-bar `WFSafe_RSI` of close is below T; short when above 100 − T (bit-exact port, `EL_WFSafeRsi_Probe.txt`) |
| `RsiRising` | Entry | mirrored | `lookback` | `lookback(14)` | Long when the N-bar `WFSafe_RSI` is above its previous-bar value; short when below — the previous value is the function's own series history, 0 on the first calculated bar |
| `CloseWithinAtrOfVwap` | Entry | mirrored | `atrLookback + 1` | `atrLookback(14)`, `multiple(0.5)` | Long when close is no more than K × ATR above the session VWAP (reset on each new date, `AvgPrice × Ticks` weighted, `AvgPrice` = (O+H+L+C)/4 or (H+L+C)/3 on a zero open); short when no more than K × ATR below it — the AI-book original's short side (`close + VWAP > …`) was degenerate and replaced by the mirror |
| `NumBarsIfProfitable` | Exit | same | 0 | `numBars(10)` | The position has been open N bars or more AND is currently profitable (open profit > 0; exactly 0 fails) — Davey exit #3 method 2; `NumBars` with a profit-sign gate, which cannot be composed from `NumBars` + another exit because exits are ORed |
| `NumBarsIfLosing` | Exit | same | 0 | `numBars(10)` | The position has been open N bars or more AND is currently losing (open profit < 0; exactly 0 fails) — Davey exit #3 method 3 |
| `ConsecutiveCloseExit` | Exit | mirrored | `consecutiveBars` | `consecutiveBars(3)` | Exit the long after N consecutive rising closes (each close above the one before), the short after N falling closes — Davey exit #6 profit-taking. Reads no position state: an exit **by placement**, the streak may pre-date the entry. Same condition as `MomentumConsecutiveBars(lookback=1)` but Exit role, and an Entry cannot sit in the exit pane |
| `TimeOfDayExit` | Exit | same | 0 | `exitTime(1605)` | The bar's close time (EL `Time`, HHMM, exchange clock) is at or past X — exits both sides at the next open, and keeps firing through the session end; intraday bars only — Davey exit #7 |
| `ProfitProtectorRatio` | Exit | same | 0 | `profitFloor(1000)`, `keepRatio(0.6)` | The **book form** of `ProfitProtector` (Davey exit #9), kept deliberately as a near-duplicate for exact testing against the book: `MaxPositionProfit >= floor` (not `>`) and `OpenPositionProfit / MaxPositionProfit < ratio` (a division, not `ratio * max`) — the two round differently and can land on opposite sides of the comparison. Nested-if structure as in the book, so the division only runs once the floor is reached |
| `TieredProfitProtector` | Exit | same | 0 | `floor1(1000)`, `floor2(2000)`, `floor3(3000)`, `ratio1(0.6)`, `ratio2(0.75)`, `ratio3(0.9)` | `ProfitProtectorRatio` with rising keep-ratios: the three floor tests assign the ratio in sequence so the highest floor reached wins, then `open / max < ratio` gated on floor1 — Davey exit #11, statement for statement; `ppratio` is a persistent variable as in the book (stale value never read, since the gate re-assigns it first) |
| `HarrisFourBarPattern` | Entry | mirrored | 3 | — | EEC #17 "Back In Style" (Michael Harris 4-bar pattern): long when high[3] < low AND low < high[1] AND high < high[2] AND high[1] < high[2]; short is the mirror (low[3] > high AND high > low[1] AND low > low[2] AND low[1] > low[2]). One pattern — the four comparisons are not independently meaningful, so not split |
| `PitterPatterPattern` | Entry | mirrored | 1 | — | EEC #29 "Pitter Patter Pattern" (Harris 2-bar pattern): long when open[1] > high AND open > close[1] AND close[1] > low[1] AND low[1] > close; short is the mirror. The near-tautological close[1] > low[1] term is kept because it is the book's rule |
| `HarrisThreeBarPattern` | Entry | mirrored | 3 | — | EEC #30's pattern term: long when low[3] > high AND high > low[1] AND low[1] > low[2]; short is the mirror. `HarrisFourBarPattern`'s SHORT pattern used on the LONG side, minus its low > low[2] term — a different rule, not a flip |
| `ClosingPatternOne` | Entry | mirrored | 3 | — | EEC #31 "Closing Pattern Only": long when close[1] > close[3] AND close > close[2] AND close[2] > close[1] (the ordering close > close[2] > close[1] > close[3]); short is the mirror |
| `ClosingPatternTwo` | Entry | mirrored | 5 | — | EEC #33 "Closing Pattern Only II": long when close[1] < close[2] AND close[2] < close[5] AND close[5] < close[3] AND close[3] < close[4]; short is the mirror. Never reads close[0], so the signal is known a bar early |
| `InsideBar` | Entry | same | `lag + 1` | `lag(1)` | The bar N bars ago is an inside bar — its high is below and its low above the bar before it (high[N+1] > high[N] AND low[N+1] < low[N]); identical both sides. EEC #32's pullback term |
| `CloseAbovePriorHighestHigh` | Entry | mirrored | `lookback` | `lookback(10)` | Close is above the highest high (long) / below the lowest low (short) of the N bars ENDING ONE BAR AGO — EL's `Highest(High, N)[1]`, bars 1..N, excluding the current bar. Not `Breakout` (closes, includes the current bar, `>=`) and not `CloseAbovePriorHigh` (a single bar). EEC #37's breakout term |
| `CloseRangePosition` | Entry | mirrored | 0 | `threshold(0.5)`, `epsilon(0.000001)` | Long when the close sits in the upper part of its own bar, (close − low)/(high − low + ε) > T; short when below 1 − T. The ε is the book's literal, reproduced as an input so the ratio's low bits match. EEC #38's "strong close" term (EEC #39 uses the same ratio with the opposite sense and no ε) |
| `AwesomeOscillatorSlope` | Entry | mirrored | `max(fastLength + extraLength - 1, max(nearLag, farLag))` | `fastLength(5)`, `extraLength(2)`, `nearLag(1)`, `farLag(1)` | Bill Williams' Awesome Oscillator, AO = Average((H+L)/2, F) − Average((H+L)/2, F+X), is falling (long: AO[near] < AO[far]) / rising (short: AO[near] > AO[far]) — the sides follow EEC #38's divergence use, long on a falling AO. AO is an EL VARIABLE, so its lags read zero-filled warm-up history; the two averages are plain `Average` over a price expression, recomputed fresh. **At the book's defaults nearLag = farLag = 1 the rule compares AO[1] with itself and never fires** — kept as printed, the lags are meant to be optimized |
| `EecQuickPullbackPattern` | Entry | mirrored | 2 | — | EEC #32 "Quick Pullback Pattern", the book's rule kept WHOLE for testing against the book (deliberately not split): long when high[2] > high[1] AND low[2] < low[1] AND close > high[2]; short when low[2] < low[1] AND high[2] > high[1] AND close < low[2]. Equals `InsideBar(1)` AND `CloseAbovePriorHigh(2)` |
| `EecPitterPatterPattern2` | Entry | mirrored | 3 | — | EEC #30 "Pitter Patter Pattern 2", kept WHOLE for testing against the book (deliberately not split): long when low[3] > high AND high > low[1] AND low[1] > low[2] AND close > close[1]; short is the mirror. Equals `HarrisThreeBarPattern` AND `Momentum(1)` |
| `EecNewHighConsecutiveHighs` | Entry | mirrored | `max(xbars, 3)` | `xbars(10)` | EEC #37 "New High With Consecutive Highs", kept WHOLE for testing against the book (deliberately not split): long when close > Highest(High, xbars)[1] AND close > close[1] AND close > close[3] AND close[1] > close[2]; short is the mirror with Lowest(Low, xbars)[1]. Equals `CloseAbovePriorHighestHigh(xbars)` AND `MomentumConsecutiveBars(1, 2)` AND `Momentum(3)` |
| `EecAwesomeOscillator` | Entry | mirrored | `max(v1 + v3 - 1, max(aback, bback))` | `aback(1)`, `bback(1)`, `v1(5)`, `v3(2)`, `fatr(0.5)` | EEC #38 "Start With an Awesome Oscillator", kept WHOLE for testing against the book (deliberately not split), with the book's variable names and its literal .000001: AO = Average((H+L)/2, v1) − Average((H+L)/2, v1+v3) as an EL variable; long when AO[aback] < AO[bback] AND low < low[1] AND (close − low)/(high − low + .000001) > fatr; short when AO[aback] > AO[bback] AND high > high[1] AND the ratio < 1 − fatr. **With the printed defaults aback = bback = 1 both AO comparisons are AO[1] vs itself and the rule NEVER FIRES** — kept as printed, not "fixed". The book's unused `Price` variable is omitted (no behavioural effect). Equals `AwesomeOscillatorSlope(v1, v3, aback, bback)` AND `LaggedHigherHigh(0)` flipped AND `CloseRangePosition(fatr, .000001)` |
| `FlatOrOpenLossExceeds` | Entry | same | 0 | `openloss(1000)` | Flat, or the open position's loss exceeds X dollars — the reversal permission from Entry & Exit Confessions #1. An Entry that reads position state, deliberately: it decides whether a fresh signal may reverse the open trade |
| `DayOfWeekAnyOf` | Entry | same | 0 | `day1(2)`, `day2(3)`, `day3(4)` | The bar's close date falls on any of three weekdays (EL numbering, 0 = Sunday) — an OR of days, which `DayOfWeekEntry`'s one-day-per-side form cannot express across AND-ed panes. Repeat a day to use fewer: (5, 1, 1) is Fri/Mon. A set, not a range, so Sunday-night bars are excluded as the book's code excludes them |
| `HighestHighBreakout` | Entry | mirrored | `lookback - 1` | `lookback(15)` | High is at or above the highest high of the last N bars (long) / low at or below the lowest low (short) — the `=` trap on highs, exactly as `Breakout` handles it on closes (Extremes loop, `el_eq` against a bar-0-seeded extreme) |
| `AtrDollarsBelow` | Entry | same | `atrLookback + 1` | `atrLookback(14)`, `maxDollars(2500)` | The N-bar ATR (`WFSafe_AvgTrueRange` rolling form) × BigPointValue (unconverted) is below X dollars — a low-volatility filter, identical both sides |
| `SeasonalHalfYear` | Entry | mirrored | 0 | `lastLongMonth(6)` | Long when the bar's calendar month is ≤ N, short when > N — the book's January–June long / July–December short split, one input |
| `FlatOnly` | Entry | same | 0 | — | `MarketPosition = 0` at SIGNAL time, both sides. This is the book's entry-only gate (Entry & Exit Confessions exit #1), mirrored as written; it is NOT TS's fill-time no-pyramiding rule (`SimOptions::max_entries_per_direction`) and suppresses the same-fill close-and-re-open that rule allows |
| `EquityDrawdownFilter` | Entry | same | `lookback` (variable history, not price) | `lookback(200)`, `lossAmount(3000)` | Strategy equity (NetProfit + OpenPositionProfit, an EL variable) has not fallen more than X dollars over the last N bars. Reads the variable's zero-filled history, so for the first N evaluated bars the comparison is against 0 equity. An entry filter, not a Switch — it never forces flat |
| `EecGoWithTheFlow` | Entry | mirrored | 1 | `openloss(1000)` | Entry & Exit Confessions #1 verbatim, deliberately NOT split: close above the previous close AND (flat OR open loss beyond X). Equals `Momentum(1)` + `FlatOrOpenLossExceeds`; kept whole to test the book's exact rule |
| `EecEveryoneLovesFriday` | Entry | mirrored | `bbars - 1` | `bbars(25)` | Entry & Exit Confessions #2 verbatim, deliberately NOT split: Friday AND close at the highest (long) / lowest (short) close of N bars. Equals `DayOfWeekEntry(5,5)` + `Breakout` |
| `EecSplitWeekPart1` | Entry | mirrored | `max(bbars - 1, atrLookback + 1)` | `bbars(15)`, `maxl(2500)`, `atrLookback(14)` | Entry & Exit Confessions #14 verbatim, deliberately NOT split: Tue/Wed/Thu AND highest high AND highest close of N bars AND ATR × BigPointValue below X. Equals `DayOfWeekAnyOf(2,3,4)` + `HighestHighBreakout` + `Breakout` + `AtrDollarsBelow` |
| `EecSplitWeekPart2` | Entry | mirrored | `max(bbars - 1, atrLookback + 1)` | `bbars(15)`, `maxl(2500)`, `atrLookback(14)` | Entry & Exit Confessions #15 verbatim, deliberately NOT split: as Part 1 but Fri/Mon. Equals `DayOfWeekAnyOf(5,1,1)` + the rest |
| `EecRsiTrigger` | Entry | mirrored | `max(rsiLength, xBars - 1)` | `rsiLength(5)`, `rsiThreshold(80)`, `xBars(5)` | Entry & Exit Confessions #12 verbatim, deliberately NOT split: `WFSafe_RSI` below T (long) / above 100 − T (short) AND close above (long) / below (short) its N-bar average. Equals `RsiBelowThreshold` + `CloseAboveAverage` |
| `EecThreeAmigos` | Entry | mirrored | `max(adxLength, rsiLength, lookbackBig, lookbackShort)` | `adxLength(14)`, `rsiLength(14)`, `lookbackBig(20)`, `lookbackShort(10)`, `adxThreshold(25)` | Entry & Exit Confessions #27 verbatim, deliberately NOT split: `WFSafe_ADX` strictly above 25 AND `WFSafe_RSI` below 50 (long) / above 50 (short) AND close below (long) the close N bars ago AND above (long) the close M bars ago — a pullback within a trend. Near `AdxBelowThreshold` negated (that gives ≥) + `RsiBelowThreshold(50)` + `Momentum` flipped + `Momentum` |
| `EecTwoAmigos` | Entry | mirrored | `max(adxLength, lookback)` | `adxLength(14)`, `lookback(20)`, `adxThreshold(20)` | Entry & Exit Confessions #28 verbatim, deliberately NOT split: `WFSafe_ADX` strictly above 20 AND close above (long) / below (short) the close N bars ago. Near `AdxBelowThreshold` negated + `Momentum` |
| `EecDayOfWeekWithATwist` | Entry | one day per side | `lookback` (variable history) | `lookback(200)`, `lossAmt(3000)` | Entry & Exit Confessions bonus #2 verbatim, deliberately NOT split: equity has not lost more than X over N bars AND Thursday (long, fills Friday's open) / Friday (short, fills Monday's open). Equals `EquityDrawdownFilter` + `DayOfWeekEntry(4,5)` |
| `TimeWindow` | Entry | same | 0 | `startTime(1600)`, `endTime(2300)` | The bar's close time (EL `Time`, HHMM, exchange clock) is inside startTime..endTime, boundaries INCLUSIVE — identical both sides. EEC #18 uses it NEGATED: the book's `time < 1600 or time > 2300` is exactly NOT(1600 <= Time <= 2300), so the per-placement negate toggle on `TimeWindow(1600, 2300)` reproduces it, boundary bars and all |
| `TwoBarRangePosition` | Entry | mirrored | 1 | `threshold(0.5)` | Where the close sits in the TWO-bar range, ll = MinList(Low, Low[1]), hh = MaxList(High, High[1]): long (buy weakness) when (close − ll)/(hh − ll + .000001) <= 1 − T, short when >= T — mean-reversion, per EEC #18. The .000001 is the book's literal, hard-coded as a structural constant, not an input. At T = 0.5 a ratio of exactly 0.5 satisfies BOTH sides on the same bar — the book's behaviour, kept. MinList/MaxList are ACCEPTED for tick-grid price operands only, which these are. Not `CloseRangePosition` (single-bar range, strict >/<, trend sense) |
| `EecWhereYouAt` | Entry | mirrored | 1 | `threshold(0.5)`, `startTime(1600)`, `endTime(2300)` | EEC #18 "Where You At?" verbatim, deliberately NOT split — book composites are tested as-is: outside the time window (Time < startTime or Time > endTime) AND the 2-bar range-position test with the book's .000001. Equals `TimeWindow(1600, 2300)` negated + `TwoBarRangePosition(0.5)`. At threshold 0.5 both sides can fire on the same bar |
| `CalendarBoundaryExit` | Exit | same | 1 (the `Date` series) | `exitMonth(7)`, `priorMonth(6)` | EEC X4 "Timed Exit, by Date/Time", the book's exact condition: `year(date) <> year(date[1]) or (month(date) = exitMonth and month(date[1]) = priorMonth)` — exits both sides at the next open; at the defaults it fires twice a year (first bar whose year changed, and the first July bar after a June bar). EL `Year(Date)` returns year − 1900, but only inequality is tested, so the C++ compares civil years directly; `date[1]` is a DATA series with real history (MaxBarsBack bars precede the first evaluated bar), so `ctx.Time(1)` needs no first-bar guard |
| `ClosePercentileValue` | Entry | mirrored, one percentile input per side | `lookback - 1` | `percentileLow(0.25)`, `percentileHigh(0.75)`, `lookback(25)` | Close is above the interpolated 75th-percentile VALUE of the last N closes (long) / below the 25th (short) — EL's `Percentile` (VERIFIED: sort bars 0..N−1, current bar INCLUDED; rank = pct·(N−1); linear interpolation, Excel PERCENTILE.INC; pct a 0..1 fraction, out-of-range → −1 sentinel). NOT `ClosePercentileRank` — that ranks the close itself by counting; this interpolates a value on the sorted window. EEC #6's percentile term |
| `ClosePercentileExit` | Exit | mirrored | `lookback - 1` | `exitPercentile(0.5)`, `lookback(5)` | EEC X5 "Percentile Exit": exit the long when close is below the interpolated N-bar percentile VALUE of closes (default the median of 5), the short when above it — same VERIFIED `Percentile` model as `ClosePercentileValue`. Reads no position state: an exit by placement. The input is `exitPercentile`, not `percentile` — that name is the EL word and the feature gate rejects it |
| `EecPercentRanker` | Entry | mirrored, one percentile input per side | `max(adxLength, xbars - 1)` | `adxLength(14)`, `adxLow(20)`, `adxHigh(30)`, `percentileLow(0.25)`, `percentileHigh(0.75)`, `xbars(25)` | EEC #6 "Percent Ranker" verbatim, deliberately NOT split — book composites are tested as-is: `WFSafe_ADX` strictly above 20 AND strictly below 30 (both strict, exactly as the book writes them) AND close above the interpolated 75th-percentile value of the last N closes (long) / below the 25th (short). ADX is the bit-exact `WFSafe_ADX` port copied from EecThreeAmigos. Equals `AdxBelowThreshold(20)` negated + `AdxBelowThreshold(30)` + `ClosePercentileValue` |
| `MovingAverageCross` | Entry | mirrored | `max(fastLength, slowLength) - 1` | `fastLength(10)`, `slowLength(20)` | The fast average of close crosses ABOVE the slow average (long) / BELOW it (short) — EL's `crosses` STATE MACHINE (VERIFIED): fires when the toleranced relation flips against the most recent NON-equal side, equality runs are bridged without re-firing, and no fire until a non-equal relation has been seen (state starts UNKNOWN); averages are EL's fresh `Average`, summed newest-first. A crossing, never split. E13's cross term; E3 uses it FLIPPED |
| `CloseNearPriorLow` | Entry | mirrored | 1 | `threshold(3)` | Close is below the prior bar's low + X (long) / above the prior bar's high − X (short) — X in price points, instrument-specific; E13's XX spike filter. Not `CloseAbovePriorHigh` (opposite sense, no offset) |
| `EecBooksCanBeGreat` | Entry | mirrored | `max(shortma, longma) - 1` | `shortma(5)`, `longma(10)` | EEC E3 "Books Can Be Great" verbatim, deliberately NOT split: the shortma average of close crosses BELOW the longma average AND close < the shortma average (long); short is the mirror — countertrend, and the shared shortma input drives BOTH the cross and the filter, as the book has it. Equals `MovingAverageCross(shortma, longma)` FLIPPED + `CloseAboveAverage(shortma)` flipped |
| `EecMovingAverageCrossTwist` | Entry | mirrored | `max(max(fastLength, slowLength) - 1, 1)` | `fastLength(10)`, `slowLength(20)`, `xx(3)` | EEC E13 "Moving Average Cross, With A Twist" verbatim, deliberately NOT split: the fast average crosses ABOVE the slow AND close < low[1] + XX (long); short is the mirror (crosses below, close > high[1] − XX) — the XX gate filters crosses caused by price spikes. Equals `MovingAverageCross` + `CloseNearPriorLow(xx)` |
| `CciAverageExtreme` | Entry | mirrored | `cciLength - 1` | `cciLength(14)`, `avgLength(9)`, `threshold(100)` | EEC E35: the N-bar Average of CCI(cciLength) is at or below −T (long) / at or above +T (short) — countertrend, the book's `<=`/`>=` kept exactly. CCIValue is an EL VARIABLE, so the average reads its zero-filled warm-up history (mirrored ring buffer, re-seeded on a length change); the CCI itself is fresh from prices, window current-inclusive, summations newest-first per the register |
| `TailBarCountDominance` | Entry | mirrored | `period + 1` | `period(10)`, `threshold(5)` | EEC E36: CountIF over the last N bars (current-inclusive) of bull-tail bars (up close, lower wick longer than the body, higher high) vs the mirrored bear-tail count (down close, upper wick longer, lower low); long when the bull count beats the bear count AND exceeds T, short mirrored. The counted condition is a true SERIES — its [1] offsets shift per counted bar; all counted operands are tick-grid prices or differences of two, so the el_* tolerance is inert there |
| `UltimateOscillatorExtreme` | Entry | mirrored | `xbars + max(len1, len2, len3) - 1` | `len1(7)`, `len2(14)`, `len3(28)`, `xbars(10)` | EEC B1: `UltimateOscillator(len1,len2,len3)` — a FRACTION 0..1, NOT the ×100 textbook value — is at its lowest (long) / highest (short) of its own last xbars values, countertrend. The book's `=` becomes el_le/el_ge, and the oscillator is NOT tick-grid, so on the fraction scale the absolute 2.22e-12 tolerance genuinely bites. The function has REAL price history, so each lag is recomputed from prices (nested loops), not a zero-filled variable buffer. The book's `UltimateOsc` spelling does not verify in TS; the twin uses the real built-in name |
| `BollingerBandCross` | Entry | mirrored | `length - 1` | `length(20)`, `numDevs(2)` | Close crosses OVER the lower Bollinger band (long) / UNDER the upper (short) -- bands = fresh N-bar Average of close +/- K x POPULATION StdDev, both summed newest-first (`bollingerband` VERIFIED). The cross is EL's state machine (`crosses over`/`crosses under` VERIFIED): fires when the toleranced comparison holds and the most recent non-equal bar sat on the other side; equality runs are bridged (above-equal-above does not re-fire); the side starts UNKNOWN, so no fire on the first evaluated bar. EEC E25's band-cross term |
| `KeltnerChannelCross` | Entry | mirrored | `length + 1` | `length(20)`, `numAtrs(2)` | Close crosses OVER the lower Keltner band (long) / UNDER the upper (short) -- bands = fresh N-bar Average(close) -/+ K x ATR, the ATR as `WFSafe_AvgTrueRange`'s rolling accumulator (MW's standing override; the book prints plain `AvgTrueRange`). Same cross state machine as `BollingerBandCross`, no fire on the first evaluated bar. EEC E26's band-cross term |
| `EecClassicBollingerBands` | Entry | mirrored | `max(length - 1, length2)` | `length(20)`, `numDevs(2)`, `length2(10)` | EEC E25 "Classic Bollinger Bands" verbatim, deliberately NOT split -- book composites are tested as-is: the Bollinger band cross AND close above (long) / below (short) the close length2 bars ago (the book's plain-English text omits the momentum term; its code has it). Equals `BollingerBandCross` + `Momentum(length2)` |
| `EecClassicKeltnerChannel` | Entry | mirrored | `max(length + 1, length2)` | `length(20)`, `numAtrs(2)`, `length2(10)` | EEC E26 "Classic Keltner Channel" verbatim, deliberately NOT split: the Keltner band cross AND close above (long) / below (short) the close length2 bars ago -- ATR as `WFSafe_AvgTrueRange` (MW convention; the book prints plain `AvgTrueRange`). Equals `KeltnerChannelCross` + `Momentum(length2)` |

| `StochasticCross` | Entry | mirrored | `sLength - 1` | `sLength(8)`, `smoothing1(23)`, `smoothing2(22)` | Long when SlowK crosses over SlowD, short when under — the measured type-1 chain (FastK fresh over real history; SlowK = FastD = ratio of sums; SlowD = seed-padded mean), with EL's bridging cross state machine; SmoothingType pinned to 1, the only measured type. EEC E23, the whole book entry |
| `MoneyFlowCross` | Entry | one threshold input per side | `length` | `length(14)`, `oversold(20)`, `overbought(80)` | Long when MoneyFlow(N) crosses above oversold, short when below overbought — TP × total Ticks, ties to the else/negative branch, unclamped ratio (leaves 0..100 on negative back-adjusted prices), bridging cross state per line. Sides mirror only when overbought = 100 − oversold. EEC E24, the whole book entry |

| `LastTradeCooldown` | Entry | same | 0 | `waitAfterWin(5)`, `waitAfterLoss(20)` | The last closed position was a winner and >= X bars have passed since its exit, or it lost and >= Y bars have passed, or no trade has closed yet — EEC E16's gate on the measured PositionProfit(1)/BarsSinceExit/TotalTrades accessors; a signal-time gate that also holds back reversals, as the book's EL does |
| `EecSerialCorrelation` | Entry | mirrored | `xbars - 1` | `xbars(15)`, `waitAfterWin(5)`, `waitAfterLoss(20)` | EEC E16's EXACT composite, deliberately NOT split: the cooldown gate AND close at the lowest (long) / highest (short) close of the last N bars — countertrend, the `=` trap as el_le/el_ge |

| `EecSecondVerse` | Entry | mirrored (each side restricted to half the year) | `xbar - 1` | `xbar(10)`, `thresh(0.5)`, `lastLongMonth(6)` | EEC E39's composite, deliberately NOT split: months 1..N long-only on a highest close finishing in the LOWER part of its bar, months N+1..12 short-only mirrored. **Deliberate divergence**: both twins add E38's +.000001 epsilon to the range divisor — the book's own code divides by zero on a zero-range bar and EL raises there (Brian's call, 2026-08-31) |

## Keeping this current

Append a row whenever a rule is added to `rules/`. It is maintained by hand (and
by `/author-rule`); nothing reads it at runtime, so a stale row costs nothing but
a missed duplicate.
