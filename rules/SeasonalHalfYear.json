{
    "name": "SeasonalHalfYear",
    "type": "Entry",
    "inputVariables": {
        "lastLongMonth": "6"
    },
    "localVariables": {
        "mon": {
            "type": "int",
            "init": "0"
        }
    },
    "classMembersHook": "",
    "startOfFileHook": "",
    "preConditionHook": "// EL Month(Date): calendar month of the bar's wall-clock close date\n// (EL_Date_Probe.txt); MonthlyProfitTarget reads it the same way.\nmon = civil_from_days(day_of(ctx.Time(0))).month;",
    "longCondition": "el_le(mon, lastLongMonth)",
    "shortCondition": "el_gt(mon, lastLongMonth)",
    "postConditionHook": "",
    "endOfFileHook": ""
}
