{
    "name": "MomentumChange",
    "type": "Entry",
    "inputVariables": {
        "lookback": "50"
    },
    "localVariables": {},
    "classMembersHook": "",
    "startOfFileHook": "",
    "preConditionHook": "",
    "longCondition": "close[0] > close[lookback] && !(close[1] > close[lookback + 1])",
    "shortCondition": "close[0] < close[lookback] && !(close[1] < close[lookback + 1])",
    "postConditionHook": "",
    "endOfFileHook": ""
}
