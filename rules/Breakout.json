{
    "name": "Breakout",
    "type": "Entry",
    "inputVariables": {
        "lookback": "50"
    },
    "localVariables": {
        "highestClose": {
            "type": "double",
            "init": "0.0"
        },
        "lowestClose": {
            "type": "double",
            "init": "0.0"
        }
    },
    "classMembersHook": "",
    "startOfFileHook": "",
    "preConditionHook": "highestClose = close[0];\nlowestClose = close[0];\nfor (int i = 1; i < (int)lookback; ++i) {\n    if (close[i] > highestClose) highestClose = close[i];\n    if (close[i] < lowestClose) lowestClose = close[i];\n}",
    "longCondition": "close[0] >= highestClose",
    "shortCondition": "close[0] <= lowestClose",
    "postConditionHook": "",
    "endOfFileHook": ""
}
